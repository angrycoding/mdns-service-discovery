{
  "name": "mdns-service-discovery-client",
  "version": "0.0.0",
  "description": "mdns-service-discovery-client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/angrycoding/mdns-service-discovery"
  },
  "author": "Ruslan Matveev aka Angrycoding <ruslan.matveev@mediafellows.com>",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && yarn tsc",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "randexp": "^0.5.3"
  }
}
